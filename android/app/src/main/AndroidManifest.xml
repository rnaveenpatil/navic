<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navic">

    <!-- Essential Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Remove this - not needed and causes issues -->
    <!-- <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" /> -->

    <!-- CALL & SMS PERMISSIONS -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- For background location (only for Android 10+) -->
    <!-- Only add if you specifically need background location -->
    <!-- <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> -->

    <!-- For foreground service notification -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- For Android 10+ location accuracy -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Wi-Fi permissions -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Remove hardware requirements - make them optional -->
    <!-- GNSS Hardware Features - Make them optional (not required) -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />

    <!-- Important: Add this to allow cleartext traffic for development -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:label="NAVIC"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/LaunchTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Enable hardware acceleration -->
        <meta-data android:name="android.hardware.accelerometer" android:value="true" />
        <meta-data android:name="android.hardware.location.gps" android:value="true" />
        <meta-data android:name="android.hardware.sensor.compass" android:value="true" />

        <!-- Network Security Configuration for Android 9+ -->
        <!-- This line is already included in the application tag above -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Deep linking support -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="navic.app" />
            </intent-filter>

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <meta-data
                android:name="io.flutter.embedding.android.SplashScreenDrawable"
                android:resource="@drawable/launch_background" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Provider for location updates -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Remove or comment out this service if you don't have a custom LocationService -->
        <!-- <service
            android:name="com.example.navic.LocationService"
            android:enabled="true"
            android:exported="false" /> -->

        <!-- Google Play Services for location (optional) -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- For Flutter plugins that need this -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_GOOGLE_MAPS_API_KEY" />

        <!-- Prevent screen orientation changes during splash -->
        <meta-data
            android:name="io.flutter.embedding.android.SplashScreenDrawable"
            android:resource="@drawable/launch_background" />

        <!-- Add this for better location accuracy -->
        <meta-data
            android:name="com.google.android.gms.awareness.API_KEY"
            android:value="YOUR_AWARENESS_API_KEY" />

        <!-- For high-accuracy location -->
        <meta-data
            android:name="android.hardware.location.gps"
            android:value="true" />

        <!-- For compass -->
        <meta-data
            android:name="android.hardware.sensor.compass"
            android:value="true" />

        <!-- For accelerometer -->
        <meta-data
            android:name="android.hardware.sensor.accelerometer"
            android:value="true" />
    </application>
</manifest>